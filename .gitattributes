# Normalize line endings to LF in the repository for all text files.
* text=auto eol=lf

# Rust and config are text.
*.rs    text eol=lf
*.toml  text eol=lf
*.lock  text eol=lf
*.md    text eol=lf
*.yml   text eol=lf

# Binary assets (none yet, but future-proofed).
*.png   binary
*.vsix  binary
